import { ConfirmNewPasswordDirective } from './confirm-new-password.directive';

describe('ConfirmNewPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmNewPasswordDirective();
    expect(directive).toBeTruthy();
  });
});
